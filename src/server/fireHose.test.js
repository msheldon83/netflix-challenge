import FireHose from './fireHose.js'
import td from 'testdouble'

test ('calling start a second time does nothing', () => {
    
})

test ('on message calls sends messages', () => {
    
})

test ('on error restarts', () => {

});

test ('stop closes EventSource', () => {
    
});